# Python
.venv/
__pycache__/
*.pyc

# 编译产物 / 大文件 (模型权重不进仓库)
bin/
engine/
onnx/
dataset/
*.engine
*.onnx
trt_timing_cache.cache

# WebUI 运行时状态 (config/token/profile 参数/历史), 只留在设备上
webui/data/

# arena trace 过程可视化输出 (CSV/PNG/基线留档, 只留在本地)
arena/trace_out/
